kind: pipeline
name: default

steps:
  - name: restore-cache
    image: drillster/drone-volume-cache
    settings:
      restore: true
      mount:
        - ./node_modules
    volumes:
      - name: cache
        path: /cache

  - name: frontend
    image: node:10.16.0
    commands:
      - cd frontend
      - npm install
      - npm test
      - npm run build

  - name: backend
    image: maven:3-jdk-11
    commands:
      - cd backend
      - mvn package -B

  - name: rebuild-cache
    image: drillster/drone-volume-cache
    settings:
      rebuild: true
      mount:
        - ./node_modules
    volumes:
      - name: cache
        path: /cache

volumes:
  - name: cache
    host:
      path: /tmp/drone/cache
